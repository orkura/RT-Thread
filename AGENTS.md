# AGENTS

## 文件变更确认规则

- 除非用户在当前请求中明确授权，否则不得自动修改任何文件，也不得执行任何 Git 操作，包括暂存、提交、切换分支、合并、拉取、推送、重置和清理等。
- 在创建、修改或删除文件前，必须列出拟操作文件的名称和相对项目的路径。
- 修改内容较短时(字数在60字以内)，直接给出拟修改内容；内容较长时，简要说明修改范围、主要内容及影响。
- 说明拟执行的操作及其影响后，必须等待用户明确确认，方可执行。

## 项目运行环境

- 当前系统为Windows系统时，按照文件 [windows.md](.agents/platform/windows.md) 进行操作；

## Git 分支管理规则

- 创建或重命名 Git 分支前，必须完整阅读并遵守文件 [Git 分支管理规范](Docs/Git/Git分支管理规范.md)。
- 执行操作前，必须向用户说明拟使用的分支名称及其命名依据。
- 如果用户指定的分支名称不符合规范，应指出具体冲突并请求用户确认，不得擅自改名或创建。
- 如果规范文件不存在或无法读取，必须停止分支创建或重命名操作，并向用户说明原因。
- 本规则不替代“文件变更确认规则”；任何 Git 写操作仍须获得用户明确授权。

## Git 提交管理规则

- 暂存或提交文件前，必须完整阅读并遵守文件 [Git 提交管理规范](Docs/Git/Git提交管理规范.md)。
- 执行暂存或提交前，必须列出拟暂存文件的项目相对路径，并向用户展示完整的拟提交信息，包括必要的正文和尾注。
- 应使用明确的文件路径暂存变更，不得默认使用 `git add .` 或 `git add -A` 暂存整个工作区。
- 一个提交只能包含一个逻辑目标，不得混入无关修改或覆盖其他人的工作区变更。
- 未经用户明确授权，不得执行暂存、提交、修改提交、变基、合并或推送等 Git 写操作。
